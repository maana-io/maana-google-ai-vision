apiVersion: v1
kind: Service
metadata:
  name: {{ template "image-analyzer.fullname" . }}
  labels:
    app: {{ template "image-analyzer.name" . }}
    chart: {{ template "image-analyzer.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "image-analyzer.name" . }}
    release: {{ .Release.Name }}
